# Plan 16-04: Full Test Coverage + Build Verification

**Phase:** 16 — Voice Live Refactor
**Requirement:** VL-16-06
**Depends on:** 16-01, 16-02, 16-03
**Autonomous:** false (human verification gate)

## Goal

Comprehensive testing and verification of all Phase 16 changes.

## Tasks

### Task 1: Frontend tests

- Test `encodePcmToBase64` utility function
- Test `useVoiceSessionLifecycle` hook (mock WebSocket)
- Test `AssignHcpDialog` component (render, filter, assign action)
- Test voice constants are exported correctly
- `npx tsc -b` passes
- `npm run build` passes

### Task 2: Backend integration tests

- Test WebSocket dual-mode: mock agent connect path
- Test WebSocket fallback: agent fails → model mode
- Test token broker agent_mode_available flag
- Test build_voice_live_metadata with VoiceLiveInstance
- Test VL Instance update triggers agent re-sync
- `ruff check .` + `ruff format --check .` clean
- `pytest -v` passes

### Task 3: Human verification checkpoint (gate=blocking)

Verify visually:
1. VL Management page: create/edit/delete instances still works
2. VL Instance Editor: "Start Test" button connects in Model mode, audio/avatar works
3. HCP Editor: Voice tab shows correct VL Instance preview
4. HCP Editor: Playground "Start" connects in Agent mode (if agent synced) or Model mode (fallback)
5. AI Foundry Portal: open an HCP's agent → voice config is populated (not empty)

## Success Criteria

- [ ] All frontend tests pass
- [ ] All backend tests pass
- [ ] `npx tsc -b` + `npm run build` clean
- [ ] `ruff check` + `ruff format --check` clean
- [ ] Human visual verification passed
