# Plan 18-03: Integration Tests & Build Verification

## Goal
End-to-end verification of download/preview flow, frontend component tests, full build check.

## Tasks

### Task 1: Backend integration test — full upload → download flow
**File:** `backend/tests/test_materials_download.py` (extend)
- Test: upload a real PDF file → get version → download → verify bytes match
- Test: upload DOCX → download → verify Content-Type is correct
- Test: download with `?mode=inline` returns inline Content-Disposition
- Test: download_url in list materials response has correct format
- Test: storage_url absent from all API responses

### Task 2: Frontend component tests (if Playwright available)
**File:** `frontend/e2e/materials-download.spec.ts` (new, optional)
- Navigate to materials page
- Upload a test PDF
- Verify "Preview" button appears for PDF version
- Verify "Download" button appears
- Click preview → verify iframe dialog opens

### Task 3: Full build verification
```bash
# Backend
cd backend && ruff check . && ruff format --check . && python3 -m pytest -v

# Frontend
cd frontend && npx tsc -b && npm run build
```

### Task 4: Update ROADMAP progress
**File:** `.planning/ROADMAP.md`
- Update Phase 18 plans count: `3/3`
- Mark plans as `[x]` complete

## Verification
- [ ] All backend tests pass (including new download tests)
- [ ] Ruff lint + format clean
- [ ] Frontend TypeScript compiles clean
- [ ] Frontend build succeeds
- [ ] ROADMAP updated
