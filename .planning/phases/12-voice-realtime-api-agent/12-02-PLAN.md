---
phase: 12-voice-realtime-api-agent
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - frontend/src/types/hcp.ts
  - frontend/src/types/voice-live.ts
  - frontend/src/api/voice-live.ts
  - frontend/src/hooks/use-voice-token.ts
  - frontend/public/locales/en-US/admin.json
  - frontend/public/locales/zh-CN/admin.json
  - frontend/public/locales/en-US/voice.json
  - frontend/public/locales/zh-CN/voice.json
  - frontend/src/components/admin/voice-avatar-tab.tsx
  - frontend/src/components/admin/agent-tab.tsx
  - frontend/src/pages/admin/hcp-profile-editor.tsx
  - frontend/src/components/admin/hcp-table.tsx
autonomous: true
requirements:
  - VOICE-12-03
  - VOICE-12-05

must_haves:
  truths:
    - "HCP editor uses 3-tab layout (Profile, Voice & Avatar, Agent) with single react-hook-form instance spanning all tabs"
    - "Voice & Avatar tab shows voice name select, avatar character/style dropdowns, conversation parameters (temperature, turn detection, switches)"
    - "Agent tab shows auto-generated instructions preview (disabled textarea) and editable override textarea"
    - "HCP table shows Voice & Avatar column with badge pair or 'Not configured' text"
    - "All new UI text externalized via i18n with en-US and zh-CN translations"
    - "Tab switching preserves form state — no data loss when switching between tabs"
  artifacts:
    - path: "frontend/src/components/admin/voice-avatar-tab.tsx"
      provides: "Voice & Avatar settings tab content component"
      contains: "VoiceAvatarTab"
    - path: "frontend/src/components/admin/agent-tab.tsx"
      provides: "Agent instructions tab content component"
      contains: "AgentTab"
    - path: "frontend/src/pages/admin/hcp-profile-editor.tsx"
      provides: "Tabbed HCP editor with 3 tabs"
      contains: "TabsTrigger"
    - path: "frontend/src/components/admin/hcp-table.tsx"
      provides: "HCP table with Voice+Avatar column"
      contains: "voiceAvatarCol"
    - path: "frontend/src/types/hcp.ts"
      provides: "Extended HCP TypeScript types with voice/avatar fields"
      contains: "voice_name"
  key_links:
    - from: "frontend/src/pages/admin/hcp-profile-editor.tsx"
      to: "frontend/src/components/admin/voice-avatar-tab.tsx"
      via: "import and render in TabsContent"
      pattern: "VoiceAvatarTab"
    - from: "frontend/src/pages/admin/hcp-profile-editor.tsx"
      to: "frontend/src/components/admin/agent-tab.tsx"
      via: "import and render in TabsContent"
      pattern: "AgentTab"
    - from: "frontend/src/api/voice-live.ts"
      to: "backend POST /voice-live/token"
      via: "hcp_profile_id query parameter"
      pattern: "hcp_profile_id"

user_setup: []
---

<objective>
Frontend admin experience for per-HCP digital persona: extend TypeScript types and API client for voice/avatar fields, create VoiceAvatarTab and AgentTab components, rewrite HCP editor to tabbed layout, add Voice+Avatar column to HCP table, and externalize all new i18n keys.

Purpose: Admin can configure each HCP as a complete digital persona — voice, avatar, conversation parameters, and agent instructions — through an intuitive tabbed editor. The table overview shows per-HCP configuration status at a glance.

Output: Extended types/API, two new tab components, rewritten HCP editor page, extended HCP table, i18n keys in both locales.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-Coach-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-Coach-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-voice-realtime-api-agent/12-CONTEXT.md
@.planning/phases/12-voice-realtime-api-agent/12-RESEARCH.md
@.planning/phases/12-voice-realtime-api-agent/12-UI-SPEC.md
@.planning/phases/12-voice-realtime-api-agent/12-01-SUMMARY.md

<interfaces>
<!-- Extended HcpProfile type to create (from Plan 01 backend) -->
HcpProfileCreate and HcpProfileResponse now include:
  voice_name, voice_type, voice_temperature, voice_custom,
  avatar_character, avatar_style, avatar_customized,
  turn_detection_type, noise_suppression, echo_cancellation,
  eou_detection, recognition_language, agent_instructions_override

<!-- Extended VoiceLiveTokenResponse (from Plan 01) -->
VoiceLiveTokenResponse now includes:
  avatar_style, avatar_customized, voice_type, voice_temperature, voice_custom,
  turn_detection_type, noise_suppression, echo_cancellation,
  eou_detection, recognition_language, agent_instructions_override

<!-- Token broker API now accepts ?hcp_profile_id=xxx -->
POST /api/v1/voice-live/token?hcp_profile_id={id}

<!-- Existing UI components available -->
Tabs, TabsList, TabsTrigger, TabsContent from @/components/ui/tabs
Select, SelectTrigger, SelectContent, SelectItem from @/components/ui/select
Switch from @/components/ui/switch
Slider from @/components/ui/slider
Badge from @/components/ui/badge
Form, FormField, FormItem, FormLabel, FormControl, FormMessage from @/components/ui/form

<!-- AVATAR_VIDEO_CHARACTERS from research -->
harry: business, casual, youthful
jeff: business, formal
lisa: casual-sitting, graceful-sitting, graceful-standing, technical-sitting, technical-standing
lori: casual, graceful, formal
max: business, casual, formal
meg: formal, casual, business

<!-- VOICE_NAME_OPTIONS from research -->
en-US-AvaNeural, en-US-Ava:DragonHDLatestNeural, en-US-AndrewNeural, en-US-JennyNeural,
zh-CN-XiaoxiaoMultilingualNeural, zh-CN-XiaoxiaoNeural, zh-CN-YunxiNeural, zh-CN-YunjianNeural

<!-- TURN_DETECTION_TYPES from research -->
server_vad, semantic_vad, azure_semantic_vad, azure_semantic_vad_multilingual
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript types, API client, i18n keys for voice/avatar fields</name>
  <files>
    frontend/src/types/hcp.ts
    frontend/src/types/voice-live.ts
    frontend/src/api/voice-live.ts
    frontend/src/hooks/use-voice-token.ts
    frontend/public/locales/en-US/admin.json
    frontend/public/locales/zh-CN/admin.json
    frontend/public/locales/en-US/voice.json
    frontend/public/locales/zh-CN/voice.json
  </files>
  <read_first>
    frontend/src/types/hcp.ts
    frontend/src/types/voice-live.ts
    frontend/src/api/voice-live.ts
    frontend/src/hooks/use-voice-token.ts
    frontend/public/locales/en-US/admin.json
    frontend/public/locales/zh-CN/admin.json
    frontend/public/locales/en-US/voice.json
    frontend/public/locales/zh-CN/voice.json
    .planning/phases/12-voice-realtime-api-agent/12-UI-SPEC.md
  </read_first>
  <action>
**1. Extend `frontend/src/types/hcp.ts`** — add voice/avatar fields to all three interfaces:

Add to `HcpProfile` interface (after `agent_sync_error`):
```typescript
// Voice settings (D-01)
voice_name: string;
voice_type: string;
voice_temperature: number;
voice_custom: boolean;
// Avatar settings (D-03)
avatar_character: string;
avatar_style: string;
avatar_customized: boolean;
// Conversation parameters (D-01)
turn_detection_type: string;
noise_suppression: boolean;
echo_cancellation: boolean;
eou_detection: boolean;
recognition_language: string;
// Agent override (D-02)
agent_instructions_override: string;
```

Add to `HcpProfileCreate` interface (all optional with `?`):
```typescript
voice_name?: string;
voice_type?: string;
voice_temperature?: number;
voice_custom?: boolean;
avatar_character?: string;
avatar_style?: string;
avatar_customized?: boolean;
turn_detection_type?: string;
noise_suppression?: boolean;
echo_cancellation?: boolean;
eou_detection?: boolean;
recognition_language?: string;
agent_instructions_override?: string;
```

`HcpProfileUpdate` already extends `Partial<HcpProfileCreate>` so it inherits automatically.

**2. Extend `frontend/src/types/voice-live.ts`** — add per-HCP fields to `VoiceLiveToken`:
```typescript
export interface VoiceLiveToken {
  // ... existing fields ...
  // Per-HCP fields (D-08)
  avatar_style?: string;
  avatar_customized?: boolean;
  voice_type?: string;
  voice_temperature?: number;
  voice_custom?: boolean;
  turn_detection_type?: string;
  noise_suppression?: boolean;
  echo_cancellation?: boolean;
  eou_detection?: boolean;
  recognition_language?: string;
  agent_instructions_override?: string;
}
```

**3. Extend `frontend/src/api/voice-live.ts`** — pass `hcp_profile_id` to token endpoint:
```typescript
export async function fetchVoiceLiveToken(hcpProfileId?: string): Promise<VoiceLiveToken> {
  const params = hcpProfileId ? { hcp_profile_id: hcpProfileId } : {};
  const response = await apiClient.post<VoiceLiveToken>("/voice-live/token", null, { params });
  return response.data;
}
```

**4. Extend `frontend/src/hooks/use-voice-token.ts`** — accept `hcpProfileId` arg:
```typescript
export function useVoiceToken() {
  return useMutation<VoiceLiveToken, Error, string | undefined>({
    mutationFn: (hcpProfileId?: string) => fetchVoiceLiveToken(hcpProfileId),
  });
}
```

**5. Add i18n keys to `frontend/public/locales/en-US/admin.json`** — add these keys under the `"hcp"` object:
```json
"tabProfile": "Profile",
"tabVoiceAvatar": "Voice & Avatar",
"tabAgent": "Agent",
"voiceSettings": "Voice Settings",
"avatarSettings": "Avatar Settings",
"conversationParams": "Conversation Parameters",
"customVoice": "Custom voice",
"customAvatar": "Custom avatar",
"voiceName": "Voice Name",
"avatarCharacter": "Avatar Character",
"avatarStyle": "Avatar Style",
"temperature": "Temperature",
"turnDetection": "Turn Detection",
"noiseSuppression": "Noise Suppression",
"echoCancellation": "Echo Cancellation",
"eouDetection": "End-of-Utterance Detection",
"recognitionLanguage": "Recognition Language",
"autoDetect": "Auto Detect",
"autoInstructions": "Auto-generated Instructions",
"overrideInstructions": "Override Instructions",
"overridePlaceholder": "Leave empty to use auto-generated instructions",
"voiceAvatarCol": "Voice & Avatar",
"notConfigured": "Not configured",
"deleteConfirm": "Delete this HCP profile? This will also delete the linked AI Foundry Agent."
```

**6. Add zh-CN translations to `frontend/public/locales/zh-CN/admin.json`** — same keys:
```json
"tabProfile": "基本信息",
"tabVoiceAvatar": "语音和数字人",
"tabAgent": "AI 代理",
"voiceSettings": "语音设置",
"avatarSettings": "数字人设置",
"conversationParams": "对话参数",
"customVoice": "自定义语音",
"customAvatar": "自定义数字人",
"voiceName": "语音名称",
"avatarCharacter": "数字人角色",
"avatarStyle": "数字人风格",
"temperature": "对话温度",
"turnDetection": "轮次检测",
"noiseSuppression": "噪声抑制",
"echoCancellation": "回声消除",
"eouDetection": "语音终止检测",
"recognitionLanguage": "识别语言",
"autoDetect": "自动检测",
"autoInstructions": "自动生成指令",
"overrideInstructions": "自定义指令",
"overridePlaceholder": "留空则使用自动生成的指令",
"voiceAvatarCol": "语音和数字人",
"notConfigured": "未配置",
"deleteConfirm": "确定删除此 HCP 配置？关联的 AI Foundry Agent 也将被删除。"
```

**7. Add voice i18n keys to `frontend/public/locales/en-US/voice.json`** — add under `"error"`:
```json
"avatarFallback": "Avatar unavailable, switching to voice mode",
"voiceFallback": "Voice unavailable, switching to text mode"
```
And add new `"modeStatus"` object:
```json
"modeStatus": {
  "connected": "Connected",
  "degraded": "Degraded",
  "disconnected": "Disconnected"
}
```

**8. Add zh-CN voice i18n keys to `frontend/public/locales/zh-CN/voice.json`**:
```json
"avatarFallback": "数字人不可用，已切换为语音模式",
"voiceFallback": "语音不可用，已切换为文字模式"
```
And:
```json
"modeStatus": {
  "connected": "已连接",
  "degraded": "降级模式",
  "disconnected": "已断开"
}
```
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-Coach-vibe-coding/frontend && npx tsc -b --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `types/hcp.ts` HcpProfile interface contains `voice_name: string`, `avatar_character: string`, `voice_temperature: number`, `agent_instructions_override: string`
    - `types/hcp.ts` HcpProfileCreate interface contains `voice_name?: string`, `avatar_character?: string`
    - `types/voice-live.ts` VoiceLiveToken contains `avatar_style?: string`, `voice_temperature?: number`, `turn_detection_type?: string`
    - `api/voice-live.ts` `fetchVoiceLiveToken` accepts `hcpProfileId?: string` parameter and passes it as query param
    - `hooks/use-voice-token.ts` mutation accepts `string | undefined` argument
    - `admin.json` (en-US) contains keys: `tabProfile`, `tabVoiceAvatar`, `tabAgent`, `voiceSettings`, `avatarSettings`, `voiceAvatarCol`, `notConfigured`
    - `admin.json` (zh-CN) contains keys: `tabProfile` = "基本信息", `tabVoiceAvatar` = "语音和数字人"
    - `voice.json` (en-US) contains `modeStatus.connected` = "Connected" and `error.avatarFallback`
    - `voice.json` (zh-CN) contains `modeStatus.connected` = "已连接"
    - `npx tsc -b` passes without type errors
  </acceptance_criteria>
  <done>All TypeScript types extended with voice/avatar fields, API client passes hcp_profile_id, voice token hook accepts hcpProfileId, all i18n keys in en-US and zh-CN for both admin and voice namespaces</done>
</task>

<task type="auto">
  <name>Task 2: VoiceAvatarTab + AgentTab components, HCP editor tabbed rewrite, HCP table Voice+Avatar column</name>
  <files>
    frontend/src/components/admin/voice-avatar-tab.tsx
    frontend/src/components/admin/agent-tab.tsx
    frontend/src/pages/admin/hcp-profile-editor.tsx
    frontend/src/components/admin/hcp-table.tsx
  </files>
  <read_first>
    frontend/src/pages/admin/hcp-profile-editor.tsx
    frontend/src/components/admin/hcp-table.tsx
    frontend/src/components/ui/tabs.tsx
    frontend/src/components/ui/switch.tsx
    frontend/src/components/ui/slider.tsx
    frontend/src/components/ui/select.tsx
    frontend/src/components/admin/personality-sliders.tsx
    frontend/src/components/admin/objection-list.tsx
    .planning/phases/12-voice-realtime-api-agent/12-UI-SPEC.md
    .planning/phases/12-voice-realtime-api-agent/12-CONTEXT.md
  </read_first>
  <action>
**1. Create `frontend/src/components/admin/voice-avatar-tab.tsx`** (per D-01, D-03, D-04, UI-SPEC L-02):

```typescript
// VoiceAvatarTab component
// Props: form: UseFormReturn<HcpFormValues> (receives parent form instance)
// Contains 3 Cards: Voice Settings, Avatar Settings, Conversation Parameters
```

Constants at top of file:
```typescript
const AVATAR_VIDEO_CHARACTERS = [
  { character: "harry", styles: ["business", "casual", "youthful"] },
  { character: "jeff", styles: ["business", "formal"] },
  { character: "lisa", styles: ["casual-sitting", "graceful-sitting", "graceful-standing", "technical-sitting", "technical-standing"] },
  { character: "lori", styles: ["casual", "graceful", "formal"] },
  { character: "max", styles: ["business", "casual", "formal"] },
  { character: "meg", styles: ["formal", "casual", "business"] },
] as const;

const VOICE_NAME_OPTIONS = [
  { value: "en-US-AvaNeural", label: "Ava (EN-US)" },
  { value: "en-US-Ava:DragonHDLatestNeural", label: "Ava HD (EN-US)" },
  { value: "en-US-AndrewNeural", label: "Andrew (EN-US)" },
  { value: "en-US-JennyNeural", label: "Jenny (EN-US)" },
  { value: "zh-CN-XiaoxiaoMultilingualNeural", label: "Xiaoxiao Multilingual (ZH-CN)" },
  { value: "zh-CN-XiaoxiaoNeural", label: "Xiaoxiao (ZH-CN)" },
  { value: "zh-CN-YunxiNeural", label: "Yunxi (ZH-CN)" },
  { value: "zh-CN-YunjianNeural", label: "Yunjian (ZH-CN)" },
] as const;

const TURN_DETECTION_TYPES = [
  { value: "server_vad", label: "Server VAD" },
  { value: "semantic_vad", label: "Semantic VAD (gpt-realtime only)" },
  { value: "azure_semantic_vad", label: "Azure Semantic VAD (all models)" },
  { value: "azure_semantic_vad_multilingual", label: "Azure Semantic VAD Multilingual" },
] as const;

const RECOGNITION_LANGUAGES = [
  { value: "auto", labelKey: "autoDetect" },
  { value: "zh-CN", label: "Chinese (zh-CN)" },
  { value: "en-US", label: "English (en-US)" },
  { value: "ja-JP", label: "Japanese (ja-JP)" },
  { value: "ko-KR", label: "Korean (ko-KR)" },
] as const;
```

Implementation:
- **Voice Settings Card**: Switch for custom voice (D-01). When OFF: Select dropdown with VOICE_NAME_OPTIONS. When ON: Input field with placeholder "e.g., en-US-Ava:DragonHDLatestNeural". Use `form.watch("voice_custom")` to toggle.
- **Avatar Settings Card**: Switch for custom avatar (D-03). When OFF: Two Select dropdowns in `grid grid-cols-2 gap-4` — character dropdown from AVATAR_VIDEO_CHARACTERS, style dropdown dynamically filtered by selected character. When ON: Input for character name. Use `form.watch("avatar_customized")` to toggle. When character changes, auto-set style to first available style for that character.
- **Conversation Parameters Card**: Temperature Slider (0.0-1.0, step 0.1, show value). Turn detection Select. Noise suppression Switch. Echo cancellation Switch. EOU detection Switch. Recognition language Select.
- All fields use FormField/FormItem/FormLabel/FormControl/FormMessage from react-hook-form.
- All labels use `t("admin:hcp.xxx")` i18n keys from the copywriting contract.
- Switch rows use `flex items-center justify-between`.

**2. Create `frontend/src/components/admin/agent-tab.tsx`** (per D-02, D-05, UI-SPEC L-03):

```typescript
// AgentTab component
// Props: form: UseFormReturn<HcpFormValues>, profile: HcpProfile | undefined, isNew: boolean, onRetrySync: () => void, retrySyncPending: boolean
```

Contains:
- **Agent Status Card** (moved from sidebar): Reuse existing AGENT_STATUS_CONFIG pattern. Show status icon + label, agent ID with Tooltip, error message if failed, retry sync button, view in Azure Portal link. Card background color matches status (`bg` from AGENT_STATUS_CONFIG). For new profiles show info text about auto-creation.
- **Agent Instructions Card**: Disabled Textarea (6 rows, `bg-muted/50`) showing auto-generated instructions text built client-side from form values using the same template pattern as backend's `build_agent_instructions`. Below it, editable Textarea (6 rows) for `agent_instructions_override` field with placeholder from i18n `overridePlaceholder`.
- **Metadata Card**: Created at and Last Updated timestamps (only shown for existing profiles).

For the auto-generated instructions preview, build a simple client-side version:
```typescript
function buildPreviewInstructions(values: HcpFormValues): string {
  return `You are ${values.name}, a ${values.specialty} specialist.\n\nPersonality: ${values.personality_type}\n...`;
}
```

**3. Rewrite `frontend/src/pages/admin/hcp-profile-editor.tsx`** (per D-05, UI-SPEC L-01):

Major changes:
- Replace the `grid grid-cols-3 gap-6` layout with full-width Tabs layout
- Import Tabs, TabsList, TabsTrigger, TabsContent from `@/components/ui/tabs`
- Import VoiceAvatarTab from `@/components/admin/voice-avatar-tab`
- Import AgentTab from `@/components/admin/agent-tab`

Extend the zod schema (`hcpSchema`) with all voice/avatar fields:
```typescript
voice_name: z.string().default("en-US-AvaNeural"),
voice_type: z.string().default("azure-standard"),
voice_temperature: z.number().min(0).max(1).default(0.9),
voice_custom: z.boolean().default(false),
avatar_character: z.string().default("lori"),
avatar_style: z.string().default("casual"),
avatar_customized: z.boolean().default(false),
turn_detection_type: z.string().default("server_vad"),
noise_suppression: z.boolean().default(false),
echo_cancellation: z.boolean().default(false),
eou_detection: z.boolean().default(false),
recognition_language: z.string().default("auto"),
agent_instructions_override: z.string().default(""),
```

Update `form.reset()` in useEffect to include all new fields from profile.

Update `handleSubmit` to include voice/avatar fields in data.

Layout structure (per UI-SPEC L-01):
```tsx
<div className="max-w-4xl mx-auto">
  {/* Header (unchanged: back button, title, test chat, save) */}
  <Tabs defaultValue="profile">
    <TabsList className="w-full">
      <TabsTrigger value="profile">{t("admin:hcp.tabProfile")}</TabsTrigger>
      <TabsTrigger value="voice-avatar">{t("admin:hcp.tabVoiceAvatar")}</TabsTrigger>
      <TabsTrigger value="agent">{t("admin:hcp.tabAgent")}</TabsTrigger>
    </TabsList>
    <TabsContent value="profile" className="mt-4">
      <Form {...form}>
        <form onSubmit={form.handleSubmit(handleSubmit)} className="space-y-6">
          {/* Identity Card, PersonalitySliders, Knowledge Card, Interaction Rules Card */}
          {/* (all existing cards moved here unchanged) */}
        </form>
      </Form>
    </TabsContent>
    <TabsContent value="voice-avatar" className="mt-4">
      <VoiceAvatarTab form={form} />
    </TabsContent>
    <TabsContent value="agent" className="mt-4">
      <AgentTab
        form={form}
        profile={profile}
        isNew={isNew}
        onRetrySync={handleRetrySync}
        retrySyncPending={retrySyncMutation.isPending}
      />
    </TabsContent>
  </Tabs>
</div>
```

CRITICAL: The `<Form {...form}>` wrapper MUST wrap the entire Tabs, not just the Profile tab content. This ensures the single form instance spans all tabs and form state persists across tab switches (I-01, Pitfall 4). The form's `onSubmit` is NOT on a `<form>` element — the Save button calls `form.handleSubmit(handleSubmit)` directly.

Remove the right sidebar (col-span-1) — its content (Agent Status Card, Timestamps Card) is now inside AgentTab.

**4. Extend `frontend/src/components/admin/hcp-table.tsx`** (per D-06, D-07, UI-SPEC L-05):

Add "Voice & Avatar" column after "Agent Status" column:
- Header: `{t("hcp.voiceAvatarCol")}` — non-sortable, just plain text
- Cell: If profile has `voice_name` other than default empty or the profile has been explicitly configured, show two inline Badge elements:
  ```tsx
  <span className="flex items-center gap-1">
    <Badge variant="outline" className="text-xs">
      {getVoiceLabel(profile.voice_name)}
    </Badge>
    <Badge variant="outline" className="text-xs">
      {profile.avatar_character}-{profile.avatar_style}
    </Badge>
  </span>
  ```
  Helper: `getVoiceLabel(name: string)` extracts short display name (e.g., "en-US-AvaNeural" -> "Ava", "zh-CN-XiaoxiaoMultilingualNeural" -> "Xiaoxiao ML").
- If profile voice_name is empty or defaults, show: `<span className="text-xs text-muted-foreground">{t("hcp.notConfigured")}</span>`

Add `import { Badge } from "@/components/ui/badge"` at top.

Update the colSpan in empty state from 6 to 7. Update skeleton loading row to include one more td.
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-Coach-vibe-coding/frontend && npx tsc -b --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `voice-avatar-tab.tsx` exists, exports `VoiceAvatarTab`, contains `AVATAR_VIDEO_CHARACTERS`, `VOICE_NAME_OPTIONS`, `TURN_DETECTION_TYPES`
    - `voice-avatar-tab.tsx` renders Card with "Voice Settings", Card with "Avatar Settings", Card with "Conversation Parameters"
    - `voice-avatar-tab.tsx` contains Switch for `voice_custom`, Select for `voice_name`, Select for `avatar_character`, Slider for `voice_temperature`
    - `agent-tab.tsx` exists, exports `AgentTab`, contains `AGENT_STATUS_CONFIG`, disabled Textarea, editable Textarea with `agent_instructions_override`
    - `hcp-profile-editor.tsx` contains `TabsTrigger value="profile"`, `TabsTrigger value="voice-avatar"`, `TabsTrigger value="agent"`
    - `hcp-profile-editor.tsx` contains `<VoiceAvatarTab` and `<AgentTab`
    - `hcp-profile-editor.tsx` zod schema contains `voice_name`, `avatar_character`, `voice_temperature`, `agent_instructions_override`
    - `hcp-profile-editor.tsx` does NOT contain `grid-cols-3` (old 3-column layout removed)
    - `hcp-table.tsx` contains `voiceAvatarCol` column header and Badge import
    - `hcp-table.tsx` empty state colSpan is 7
    - `npx tsc -b` passes
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>VoiceAvatarTab and AgentTab components created. HCP editor rewritten to 3-tab Tabs layout with single form instance. HCP table shows Voice+Avatar column with badge pair. All i18n keys in both locales. Build passes.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/huqianghui/Downloads/1.github/AI-Coach-vibe-coding/frontend
npx tsc -b --noEmit
npm run build
```
</verification>

<success_criteria>
1. HcpProfile TypeScript type includes all 13 voice/avatar fields
2. VoiceLiveToken includes per-HCP fields
3. API client passes hcp_profile_id to token endpoint
4. VoiceAvatarTab renders voice name select, avatar character/style dropdowns, temperature slider, 5 switches, recognition language select
5. AgentTab renders agent status card, auto-generated instructions textarea (disabled), override textarea (editable)
6. HCP editor uses Tabs with 3 tabs — no more 3-column grid layout
7. HCP table has 7 columns including Voice & Avatar
8. All i18n keys present in en-US and zh-CN
9. TypeScript compiles cleanly, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/12-voice-realtime-api-agent/12-02-SUMMARY.md`
</output>
