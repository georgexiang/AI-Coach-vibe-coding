---
phase: 19-ai-coach-skill-module
plan: 05
type: execute
wave: 3
depends_on: ["19-02", "19-03", "19-04"]
files_modified:
  - frontend/src/pages/admin/skill-editor.tsx
  - frontend/src/components/shared/sop-editor.tsx
  - frontend/src/components/shared/file-tree-view.tsx
  - frontend/src/components/shared/quality-radar-chart.tsx
  - frontend/src/components/shared/quality-score-card.tsx
  - frontend/src/components/shared/conversion-progress.tsx
  - frontend/src/components/shared/publish-gate-dialog.tsx
  - frontend/src/components/shared/skill-material-uploader.tsx
autonomous: true
requirements:
  - D-09
  - D-11
  - D-17

must_haves:
  truths:
    - "Admin can edit SOP content in dual mode: manual Markdown editing and AI feedback-driven modification"
    - "Skill editor has 4 tabs: Content (SOP editor + preview), Resources (file tree + preview), Quality (L1+L2 results), Settings (metadata form)"
    - "File tree shows VSCode-style collapsible directory structure of Skill package"
    - "L2 quality scores display as radar chart with 6 dimensions"
    - "Publish gate dialog shows quality gate results before publish"
    - "Conversion progress indicator shows async conversion status"
  artifacts:
    - path: "frontend/src/pages/admin/skill-editor.tsx"
      provides: "Skill editor page with tabbed interface"
      contains: "export default function SkillEditorPage"
    - path: "frontend/src/components/shared/sop-editor.tsx"
      provides: "Dual-mode SOP editor (manual + AI feedback)"
      contains: "SopEditor"
    - path: "frontend/src/components/shared/file-tree-view.tsx"
      provides: "VSCode-style file tree component"
      contains: "FileTreeView"
    - path: "frontend/src/components/shared/quality-radar-chart.tsx"
      provides: "L2 six-dimension radar chart"
      contains: "QualityRadarChart"
  key_links:
    - from: "frontend/src/pages/admin/skill-editor.tsx"
      to: "frontend/src/hooks/use-skills.ts"
      via: "useSkill, useUpdateSkill, useCheckStructure, etc."
      pattern: "useSkill|useUpdateSkill"
    - from: "frontend/src/components/shared/quality-radar-chart.tsx"
      to: "recharts"
      via: "RadarChart component"
      pattern: "RadarChart"
---

<objective>
Build the Skill Editor page with all 4 tabs and supporting components: SopEditor (dual-mode editing per D-09), FileTreeView (VSCode-style per D-17), QualityRadarChart (L2 six-dimension per D-11), QualityScoreCard, ConversionProgress, PublishGateDialog, and SkillMaterialUploader.

Purpose: The editor is where admins spend most of their time creating, reviewing, and refining Skills. It must support the full creation-to-publish workflow.
Output: Complete Skill Editor page with all 4 tabs functional and connected to backend API.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-Coach-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-Coach-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-CONTEXT.md
@.planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-UI-SPEC.md
@.planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-01-SUMMARY.md
@.planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-04-SUMMARY.md

<interfaces>
From frontend/src/types/skill.ts (Plan 04):
```typescript
export interface Skill extends SkillListItem {
  content: string;
  resources: SkillResource[];
  versions: SkillVersion[];
  structure_check_details: string;
  quality_details: string;
}
export interface QualityDimension { name: string; score: number; verdict: string; strengths: string[]; improvements: string[]; }
export interface SkillEvaluationSummary { structure_check: {...}; quality: {...}; }
```

From frontend/src/hooks/use-skills.ts (Plan 04):
```typescript
export function useSkill(id: string | undefined): UseQueryResult<Skill>;
export function useUpdateSkill(): UseMutationResult;
export function useCheckStructure(): UseMutationResult;
export function useEvaluateQuality(): UseMutationResult;
export function useSkillEvaluation(id: string | undefined): UseQueryResult<SkillEvaluationSummary>;
export function useConversionStatus(id: string | undefined, enabled: boolean): UseQueryResult;
export function useUploadResources(): UseMutationResult;
```

From existing recharts usage (Phase 02 scoring):
```typescript
import { RadarChart, PolarGrid, PolarAngleAxis, PolarRadiusAxis, Radar, ResponsiveContainer } from "recharts";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SopEditor, ConversionProgress, SkillMaterialUploader, PublishGateDialog</name>
  <read_first>
    - frontend/src/pages/admin/hcp-profile-editor.tsx (tabbed editor pattern)
    - frontend/src/pages/admin/training-materials.tsx (material upload with react-dropzone)
    - frontend/src/components/ui/textarea.tsx (Textarea component)
    - frontend/src/components/ui/dialog.tsx (Dialog component)
    - frontend/src/components/ui/progress.tsx (Progress component)
    - frontend/src/components/ui/tabs.tsx (Tabs component)
    - frontend/src/hooks/use-skills.ts (all hooks)
    - .planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-UI-SPEC.md (interaction contracts)
  </read_first>
  <files>
    frontend/src/components/shared/sop-editor.tsx
    frontend/src/components/shared/conversion-progress.tsx
    frontend/src/components/shared/skill-material-uploader.tsx
    frontend/src/components/shared/publish-gate-dialog.tsx
  </files>
  <action>
1. Create `frontend/src/components/shared/sop-editor.tsx`:
   - Props: `{ content: string; onChange: (content: string) => void; onAiRegenerate?: (feedback: string) => Promise<void>; isRegenerating?: boolean; readOnly?: boolean }`
   - Layout per UI-SPEC Content tab (2-column grid, 3fr/2fr):
     - Left column (60%): 
       - Textarea for Markdown editing (monospace font, min-h-[500px])
       - Below textarea: AI feedback section:
         - Textarea with placeholder "Describe changes you want to make to the SOP..." (per UI-SPEC)
         - "Apply Changes" Button (per D-09: AI feedback-driven modification)
         - When clicked: call onAiRegenerate with feedback text, show loading state
         - On completion: content updates (parent handles), brief bg-primary/5 highlight animation (1500ms fade per UI-SPEC)
     - Right column (40%):
       - Rendered Markdown preview using react-markdown (already installed)
       - ScrollArea wrapping the preview content
       - Content updates in real-time as user types
   - useTranslation(["skill"]) for labels

2. Create `frontend/src/components/shared/conversion-progress.tsx`:
   - Props: `{ status: ConversionStatus | null; error?: string; onRetry?: () => void }`
   - Layout:
     - pending: Progress (indeterminate) + "Converting..." text with purple indicator
     - processing: Progress (indeterminate) + "Converting materials to skill..." text
     - completed: green Badge "Completed" + success message
     - failed: red Banner with error message + "Retry" Button (per D-08)
     - null/undefined: hidden
   - Use aria-live="polite" per accessibility requirement

3. Create `frontend/src/components/shared/skill-material-uploader.tsx`:
   - Props: `{ onUpload: (files: File[]) => void; isUploading?: boolean; acceptedTypes?: string[] }`
   - Use react-dropzone (existing pattern from training-materials.tsx):
     - Accept: .pdf, .docx, .pptx, .txt, .md (per D-05)
     - Max file size: 50MB
     - Drag-and-drop zone with dashed border, upload icon, instruction text
     - File list showing selected files before upload
     - "Upload and Convert" primary button

4. Create `frontend/src/components/shared/publish-gate-dialog.tsx`:
   - Props: `{ open: boolean; onOpenChange: (open: boolean) => void; skill: Skill; evaluation: SkillEvaluationSummary | null; onPublish: () => void; onCancel: () => void }`
   - Dialog content per UI-SPEC Quality Gate Flow:
     - Show L1 result: green/red banner
     - Show L2 result: overall score + verdict
     - If L1 FAIL: message "Please fix structure issues before publishing", only "Close" button
     - If L2 < 50: message per i18n confirm.publishBlock, only "Close" button
     - If L2 50-69: warning message per i18n confirm.publishWarning, show low-scoring dimensions, "Publish Anyway" + "Return to Edit" buttons
     - If L2 >= 70: success message, "Publish Skill" + "Cancel" buttons
     - "Publish Skill"/"Publish Anyway" button calls onPublish
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-Coach-vibe-coding/frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/shared/sop-editor.tsx contains `SopEditor` export
    - frontend/src/components/shared/sop-editor.tsx contains `onAiRegenerate` prop
    - frontend/src/components/shared/sop-editor.tsx contains `react-markdown` import or ReactMarkdown usage
    - frontend/src/components/shared/conversion-progress.tsx contains `ConversionProgress` export
    - frontend/src/components/shared/conversion-progress.tsx contains `aria-live`
    - frontend/src/components/shared/skill-material-uploader.tsx contains `react-dropzone` import
    - frontend/src/components/shared/publish-gate-dialog.tsx contains `PublishGateDialog` export
    - frontend/src/components/shared/publish-gate-dialog.tsx contains `50` (threshold check)
  </acceptance_criteria>
  <done>SopEditor with dual mode (manual + AI feedback per D-09), ConversionProgress with status indicator, SkillMaterialUploader with dropzone, PublishGateDialog with L1+L2 gate logic (D-12).</done>
</task>

<task type="auto">
  <name>Task 2: FileTreeView, QualityRadarChart, QualityScoreCard, Skill Editor page assembly</name>
  <read_first>
    - frontend/src/pages/admin/hcp-profile-editor.tsx (tabbed editor with params)
    - frontend/src/components/shared/sop-editor.tsx (just created)
    - frontend/src/components/shared/conversion-progress.tsx (just created)
    - frontend/src/components/shared/skill-material-uploader.tsx (just created)
    - frontend/src/components/shared/publish-gate-dialog.tsx (just created)
    - frontend/src/hooks/use-skills.ts (all hooks)
    - frontend/src/types/skill.ts (Skill, SkillResource types)
    - .planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-UI-SPEC.md (editor layout specs)
  </read_first>
  <files>
    frontend/src/components/shared/file-tree-view.tsx
    frontend/src/components/shared/quality-radar-chart.tsx
    frontend/src/components/shared/quality-score-card.tsx
    frontend/src/pages/admin/skill-editor.tsx
  </files>
  <action>
1. Create `frontend/src/components/shared/file-tree-view.tsx`:
   - Props: `{ resources: SkillResource[]; onSelectFile: (resource: SkillResource) => void; selectedId?: string; onUpload?: (files: File[], type: ResourceType) => void }`
   - Build tree structure from resources:
     - Group by resource_type into directories: "references/", "scripts/", "assets/"
     - Each directory is a collapsible node
     - Add virtual root "SKILL.md" node (always present, not from resources)
   - TreeNode component:
     - Folder: ChevronRight icon (rotates on expand), FolderOpen/Folder icon, name, click toggles expand
     - File: File icon (variant by extension: FileText for .md/.txt, FileSpreadsheet for .xlsx, Presentation for .pptx, FileCode for .py), name, file size
     - Selected file: bg-primary/5 border-l-2 border-primary (per UI-SPEC)
     - Indent: 20px per level (per UI-SPEC spacing exceptions)
   - Icons per file type using lucide-react:
     - .md: FileText
     - .pdf: FileText (red tint via className)
     - .pptx: Presentation
     - .docx: FileText (blue tint)
     - .py: FileCode
     - .txt: FileText
     - default: File
   - Keyboard navigation: ArrowUp/Down to move, ArrowRight to expand, ArrowLeft to collapse, Enter to select
   - aria-expanded on folders, aria-selected on active file
   - Upload button at top with dropdown for resource_type selection

2. Create `frontend/src/components/shared/quality-radar-chart.tsx`:
   - Props: `{ dimensions: QualityDimension[]; overallScore?: number }`
   - Use recharts RadarChart (existing pattern from scoring feedback page):
     ```typescript
     import { RadarChart, PolarGrid, PolarAngleAxis, PolarRadiusAxis, Radar, ResponsiveContainer } from "recharts";
     ```
   - Transform dimensions to chart data: `[{dimension: translated_name, score: dimension.score, fullMark: 100}]`
   - Translate dimension names using i18n: `t("skill:quality.dimensions." + dim.name)`
   - Chart: ResponsiveContainer width="100%" height={300}, RadarChart with:
     - PolarGrid
     - PolarAngleAxis dataKey="dimension"
     - PolarRadiusAxis domain={[0, 100]}
     - Radar with fill="var(--primary)" fillOpacity={0.3} stroke="var(--primary)" strokeWidth={2}
   - Below chart: overall score display with large number + verdict text
   - aria-label describing overall score

3. Create `frontend/src/components/shared/quality-score-card.tsx`:
   - Props: `{ dimension: QualityDimension }`
   - Card layout:
     - Header: dimension name (translated) + score badge
     - Progress bar showing score (0-100, color by verdict: green >= 70, orange 50-69, red < 50)
     - Expandable sections for strengths, improvements, critical_issues
     - Use lucide icons: Check for strengths, AlertTriangle for improvements, XCircle for critical

4. Rewrite `frontend/src/pages/admin/skill-editor.tsx` (replace placeholder):
   - Default export: `SkillEditorPage`
   - Use useParams() to get `id` (undefined for new)
   - Use useSkill(id), useUpdateSkill(), useCreateSkill(), useCheckStructure(), useEvaluateQuality(), useSkillEvaluation(id), useConversionStatus(id, isConverting), usePublishSkill(), useUploadResources()
   - Page header per UI-SPEC:
     - Back link (ArrowLeft + "Back to Skill Hub", navigate to /admin/skills)
     - Title: "Skill: {name}" or "Create New Skill"
     - "Save Draft" outline Button + "Publish Skill" primary Button
   - Tabs (Content | Resources | Quality | Settings) per UI-SPEC:

   **Content Tab:**
   - If new skill (no id): show SkillMaterialUploader + "or create empty skill" link
   - If converting (conversion_status == "processing"/"pending"): show ConversionProgress
   - If content exists: show SopEditor with content and live preview
   - AI regenerate handler:
     ```typescript
     async function handleAiRegenerate(feedback: string) {
       await updateSkill({ id, data: { content: "regenerating..." } }); // placeholder
       // TODO: Call backend AI feedback endpoint (to be added)
     }
     ```

   **Resources Tab:**
   - 2-column layout (30%/70% per UI-SPEC)
   - Left: FileTreeView with skill.resources
   - Right: File preview panel
     - SKILL.md selected: render content as Markdown
     - Reference file: show file info card (name, size, type, upload date) + download button
     - Script file: render text_content in monospace `<pre>` block
     - Asset file: file info card + download button
     - No selection: centered muted text "Select a file from the tree to preview"

   **Quality Tab:**
   - Load evaluation data with useSkillEvaluation(id)
   - L1 section: banner showing pass/fail with issues list
   - L2 section: QualityRadarChart + dimension QualityScoreCards in 2-col grid
   - "Request Quality Review" button triggers: checkStructure then evaluateQuality
   - If not evaluated: show muted card per UI-SPEC empty state

   **Settings Tab:**
   - Form (react-hook-form + zod) for skill metadata:
     - Name (Input, required)
     - Description (Textarea)
     - Product (Input)
     - Therapeutic Area (Input)
     - Tags (Input, comma-separated)
     - Compatibility (Input)
   - Save button calls useUpdateSkill

   **Publish Flow:**
   - "Publish Skill" button opens PublishGateDialog
   - Dialog shows evaluation results and applies gate logic (D-12)
   - On publish confirmation: call usePublishSkill, navigate to /admin/skills on success
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-Coach-vibe-coding/frontend && npx tsc --noEmit --pretty 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/shared/file-tree-view.tsx contains `FileTreeView` export
    - frontend/src/components/shared/file-tree-view.tsx contains `aria-expanded`
    - frontend/src/components/shared/quality-radar-chart.tsx contains `RadarChart` from recharts
    - frontend/src/components/shared/quality-radar-chart.tsx contains `QualityRadarChart` export
    - frontend/src/components/shared/quality-score-card.tsx contains `QualityScoreCard` export
    - frontend/src/pages/admin/skill-editor.tsx contains `export default function SkillEditorPage`
    - frontend/src/pages/admin/skill-editor.tsx contains `Content` and `Resources` and `Quality` and `Settings` tab labels
    - frontend/src/pages/admin/skill-editor.tsx contains `useSkill(`
    - frontend/src/pages/admin/skill-editor.tsx contains `SopEditor`
    - frontend/src/pages/admin/skill-editor.tsx contains `FileTreeView`
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>FileTreeView with VSCode-style tree (D-17), QualityRadarChart with 6 dimensions (D-11), QualityScoreCard per dimension. Complete Skill Editor with 4 tabs: Content (SOP + AI feedback D-09), Resources (file tree + preview), Quality (L1+L2 gate results), Settings (metadata form). Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> SOP content | Markdown content from textarea rendered in preview |
| AI feedback -> content update | User-provided feedback text sent to AI API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-14 | Tampering | SOP Markdown rendering | mitigate | react-markdown with rehype-raw sanitizes HTML by default. No dangerouslySetInnerHTML. |
| T-19-15 | Information Disclosure | File download | mitigate | Download URLs go through authenticated API (/api/v1/skills/{id}/resources/{rid}/download), require admin role. |
</threat_model>

<verification>
```bash
cd /Users/huqianghui/Downloads/1.github/AI-Coach-vibe-coding/frontend
npx tsc --noEmit
npm run build
```
</verification>

<success_criteria>
- Skill editor page with 4 functional tabs
- SopEditor supports manual edit + AI feedback input (D-09)
- FileTreeView with VSCode-style collapsible tree (D-17), keyboard accessible
- QualityRadarChart displays 6 L2 dimensions (D-11)
- PublishGateDialog enforces quality gates (D-12)
- ConversionProgress shows async status with retry
- TypeScript compiles, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/19-ai-coach-skill-module-skill-lifecycle-management-material-to/19-05-SUMMARY.md`
</output>
