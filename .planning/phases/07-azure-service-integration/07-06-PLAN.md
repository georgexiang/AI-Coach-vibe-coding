---
phase: 07-azure-service-integration
plan: 06
type: execute
wave: 5
depends_on:
  - "07-05"
files_modified:
  - backend/tests/test_adapters_new.py
  - backend/tests/test_region_capabilities.py
  - backend/tests/test_connection_tester.py
  - backend/tests/test_azure_config_api_extended.py
autonomous: true
requirements:
  - PLAT-03
  - ARCH-05
  - PLAT-05

must_haves:
  truths:
    - "Unit tests cover all 3 new adapters (Content Understanding, Realtime, Voice Live)"
    - "Region capabilities tests verify correct availability for known and unknown regions"
    - "Connection tester tests verify dispatch for all 7 service names"
    - "API tests verify region-capabilities endpoint and azure_openai_realtime PUT"
    - "All tests pass with mocked HTTP calls (no real Azure credentials needed)"
  artifacts:
    - path: "backend/tests/test_adapters_new.py"
      provides: "Unit tests for Content Understanding, Realtime, Voice Live adapters"
      contains: "test_content_understanding_execute"
    - path: "backend/tests/test_region_capabilities.py"
      provides: "Region capabilities module tests"
      contains: "test_get_region_capabilities"
    - path: "backend/tests/test_connection_tester.py"
      provides: "Connection tester dispatch and individual test function tests"
      contains: "test_service_connection_dispatch"
    - path: "backend/tests/test_azure_config_api_extended.py"
      provides: "API integration tests for new endpoints"
      contains: "test_region_capabilities_endpoint"
  key_links:
    - from: "backend/tests/test_adapters_new.py"
      to: "backend/app/services/agents/adapters/azure_content.py"
      via: "import and test execute/is_available"
      pattern: "AzureContentUnderstandingAdapter"
    - from: "backend/tests/test_connection_tester.py"
      to: "backend/app/services/connection_tester.py"
      via: "mock httpx calls and verify dispatch"
      pattern: "test_service_connection"
---

<objective>
Create comprehensive backend tests for all new Phase 07-05 code: adapter unit tests, region capabilities tests, connection tester tests, and API integration tests for the region-capabilities endpoint and azure_openai_realtime service.

Purpose: Ensures >=95% test coverage for all new backend code per project convention. Tests use mocked HTTP calls so no Azure credentials are needed.

Output: 4 test files covering adapters, region capabilities, connection tester, and API routes.
</objective>

<execution_context>
@/Users/huqianghui/Downloads/1.github/AI-Coach-vibe-coding/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huqianghui/Downloads/1.github/AI-Coach-vibe-coding/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-azure-service-integration/07-CONTEXT.md
@.planning/phases/07-azure-service-integration/07-05-SUMMARY.md

<interfaces>
From backend/tests/conftest.py (test fixtures pattern):
```python
# Uses pytest-asyncio with AsyncSession
# db fixture: yields in-memory SQLite async session
# admin_user fixture: creates User with role="admin"
# admin_token fixture: returns JWT for admin
# client fixture: httpx.AsyncClient with ASGITransport
```

From backend/tests/test_azure_openai_adapter.py (adapter test pattern):
```python
# Mocks openai.AsyncAzureOpenAI with unittest.mock.patch
# Uses @pytest.mark.asyncio
# Collects events from async iterator into list
# Asserts event types and content
```

From backend/tests/test_azure_config_api.py (API test pattern):
```python
# Uses client fixture from conftest
# Sends requests to /api/v1/azure-config/services/...
# Uses admin_token for auth
# Patches connection_tester functions to avoid real API calls
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit tests for new adapters and region capabilities</name>
  <files>
    backend/tests/test_adapters_new.py,
    backend/tests/test_region_capabilities.py
  </files>
  <read_first>
    backend/app/services/agents/adapters/azure_content.py,
    backend/app/services/agents/adapters/azure_realtime.py,
    backend/app/services/agents/adapters/azure_voice_live.py,
    backend/app/services/region_capabilities.py,
    backend/tests/test_azure_openai_adapter.py,
    backend/tests/conftest.py
  </read_first>
  <behavior>
    - AzureContentUnderstandingAdapter.is_available() returns True when endpoint+key set, False when empty
    - AzureContentUnderstandingAdapter.execute() yields TEXT+DONE events on success (mock httpx 202 then 200)
    - AzureContentUnderstandingAdapter.execute() yields ERROR+DONE on exception
    - AzureRealtimeAdapter.is_available() returns True when endpoint+key+deployment set
    - AzureRealtimeAdapter.is_available() returns False when deployment empty
    - AzureRealtimeAdapter.execute() yields ERROR+DONE (frontend-direct service)
    - AzureVoiceLiveAdapter.is_available() returns True when endpoint+key set
    - AzureVoiceLiveAdapter.execute() yields ERROR+DONE (frontend-direct service)
    - parse_voice_live_mode("agent:abc:proj") returns {"mode": "agent", "agent_id": "abc", "project_name": "proj"}
    - parse_voice_live_mode("gpt-realtime") returns {"mode": "model", "model": "gpt-realtime"}
    - parse_voice_live_mode("") returns {"mode": "model", "model": "gpt-4o-realtime-preview"}
    - get_region_capabilities("eastus2") has azure_avatar.available=True
    - get_region_capabilities("westus") has azure_avatar.available=False
    - get_region_capabilities("eastus2") has all 7 services in result
    - get_region_capabilities("unknown-region") has azure_openai.available=True (unrestricted)
    - get_region_capabilities("unknown-region") has azure_avatar.available=False
  </behavior>
  <action>
1. Create `backend/tests/test_adapters_new.py`:
   All tests use `@pytest.mark.asyncio`.

   **Content Understanding tests:**
   - `test_content_understanding_is_available_true`: Adapter with endpoint="https://test.com", api_key="key" -> True
   - `test_content_understanding_is_available_false`: Adapter with endpoint="", api_key="" -> False
   - `test_content_understanding_execute_success`: Mock httpx.AsyncClient to return 202 with Operation-Location header on POST, then 200 with JSON `{"status": "Succeeded", "result": {"data": "test"}}` on GET. Assert TEXT event contains result JSON, followed by DONE.
   - `test_content_understanding_execute_error`: Mock httpx to raise ConnectionError. Assert ERROR event followed by DONE.

   **Realtime tests:**
   - `test_realtime_is_available_true`: endpoint+key+deployment set -> True
   - `test_realtime_is_available_false_no_deployment`: endpoint+key set, deployment="" -> False
   - `test_realtime_execute_returns_error_done`: execute() yields ERROR with "frontend-direct" message, then DONE

   **Voice Live tests:**
   - `test_voice_live_is_available_true`: endpoint+key set -> True
   - `test_voice_live_is_available_false`: endpoint="" -> False
   - `test_voice_live_execute_returns_error_done`: execute() yields ERROR, then DONE
   - `test_parse_voice_live_mode_agent`: parse_voice_live_mode("agent:abc123:my-project") returns mode=agent, agent_id=abc123, project_name=my-project
   - `test_parse_voice_live_mode_model`: parse_voice_live_mode("gpt-realtime") returns mode=model, model=gpt-realtime
   - `test_parse_voice_live_mode_empty`: parse_voice_live_mode("") returns mode=model, model=gpt-4o-realtime-preview

   Use `unittest.mock.patch` and `unittest.mock.AsyncMock` for httpx mocking.

2. Create `backend/tests/test_region_capabilities.py`:
   - `test_get_region_capabilities_eastus2`: All 7 services present, avatar available=True, voice_live available=True
   - `test_get_region_capabilities_westus`: avatar available=False (westus not in AVATAR_REGIONS), voice_live available=False (westus not in VOICE_LIVE_REGIONS... actually check: westus IS in VOICE_LIVE_REGIONS). Verify voice_live available=True, azure_openai available=True.
   - `test_get_region_capabilities_unknown`: azure_openai available=True (unrestricted), avatar available=False
   - `test_get_region_capabilities_case_insensitive`: "EastUS2" returns same as "eastus2"
   - `test_avatar_regions_count`: AVATAR_REGIONS has exactly 7 entries
   - `test_voice_live_regions_count`: VOICE_LIVE_REGIONS has at least 20 entries
   - `test_all_service_names_in_result`: get_region_capabilities("eastus2") result has all 7 service keys
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-Coach-vibe-coding/backend && python3 -m pytest tests/test_adapters_new.py tests/test_region_capabilities.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/test_adapters_new.py contains `test_content_understanding_is_available_true`
    - backend/tests/test_adapters_new.py contains `test_content_understanding_execute_success`
    - backend/tests/test_adapters_new.py contains `test_realtime_is_available_true`
    - backend/tests/test_adapters_new.py contains `test_realtime_execute_returns_error_done`
    - backend/tests/test_adapters_new.py contains `test_voice_live_is_available_true`
    - backend/tests/test_adapters_new.py contains `test_parse_voice_live_mode_agent`
    - backend/tests/test_adapters_new.py contains `test_parse_voice_live_mode_model`
    - backend/tests/test_region_capabilities.py contains `test_get_region_capabilities_eastus2`
    - backend/tests/test_region_capabilities.py contains `test_get_region_capabilities_unknown`
    - backend/tests/test_region_capabilities.py contains `test_all_service_names_in_result`
    - `pytest tests/test_adapters_new.py tests/test_region_capabilities.py -x -v` exits 0
  </acceptance_criteria>
  <done>All adapter unit tests and region capability tests pass, covering is_available, execute, parse_voice_live_mode, and region lookup for known/unknown regions</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Connection tester tests and API integration tests</name>
  <files>
    backend/tests/test_connection_tester.py,
    backend/tests/test_azure_config_api_extended.py
  </files>
  <read_first>
    backend/app/services/connection_tester.py,
    backend/app/api/azure_config.py,
    backend/tests/test_azure_config_api.py,
    backend/tests/conftest.py
  </read_first>
  <behavior>
    - test_service_connection dispatches "azure_openai_realtime" to test_azure_realtime
    - test_service_connection dispatches "azure_content" to test_azure_content_understanding
    - test_service_connection returns (False, "Unknown service: ...") for unknown service
    - test_azure_avatar returns success when 200 from ICE token endpoint
    - test_azure_avatar returns failure when 401 from ICE token endpoint
    - test_azure_content_understanding returns success when 200 from analyzers list
    - test_azure_realtime returns success when 200, failure when 404 with deployment name
    - GET /api/v1/azure-config/region-capabilities/eastus2 returns 200 with service map
    - GET /api/v1/azure-config/region-capabilities/eastus2 requires admin role
    - PUT /api/v1/azure-config/services/azure_openai_realtime returns 200 (not 400)
  </behavior>
  <action>
1. Create `backend/tests/test_connection_tester.py`:
   Use `unittest.mock.patch` on `httpx.AsyncClient` and `openai.AsyncAzureOpenAI`.

   - `test_dispatch_azure_openai`: patch test_azure_openai, call test_service_connection("azure_openai",...), verify dispatch
   - `test_dispatch_azure_openai_realtime`: patch test_azure_realtime, call test_service_connection("azure_openai_realtime",...), verify dispatch
   - `test_dispatch_azure_content`: patch test_azure_content_understanding, call test_service_connection("azure_content",...), verify dispatch
   - `test_dispatch_unknown_service`: call test_service_connection("unknown",...), assert returns (False, "Unknown service: unknown")
   - `test_azure_avatar_real_success`: mock httpx GET to return 200 for ICE token URL. Assert (True, "Avatar service reachable...")
   - `test_azure_avatar_real_failure`: mock httpx GET to return 401. Assert (False, "Avatar test failed: HTTP 401")
   - `test_azure_content_understanding_success`: mock httpx GET for analyzers URL to return 200. Assert (True, "Content Understanding service connected...")
   - `test_azure_content_understanding_failure`: mock httpx GET to return 403. Assert (False, "Content Understanding test failed: HTTP 403...")
   - `test_azure_realtime_success`: mock httpx GET for deployment URL to return 200. Assert (True, "Realtime API deployment verified.")
   - `test_azure_realtime_not_found`: mock httpx GET to return 404. Assert (False, "Deployment 'gpt-4o-realtime-preview' not found")

   All async tests use `@pytest.mark.asyncio`.

2. Create `backend/tests/test_azure_config_api_extended.py`:
   Use the existing conftest client/admin_token fixtures.

   - `test_region_capabilities_eastus2`: GET /api/v1/azure-config/region-capabilities/eastus2 with admin token. Assert 200. Assert response has "region" and "services" keys. Assert services.azure_avatar.available == True. Assert services.azure_openai.available == True.
   - `test_region_capabilities_unknown`: GET /api/v1/azure-config/region-capabilities/unknown-region. Assert 200. Assert services.azure_avatar.available == False. Assert services.azure_openai.available == True.
   - `test_region_capabilities_requires_admin`: GET without token returns 401 or 403.
   - `test_put_azure_openai_realtime`: PUT /api/v1/azure-config/services/azure_openai_realtime with body `{"endpoint": "https://test.openai.azure.com", "api_key": "test-key", "model_or_deployment": "gpt-4o-realtime-preview", "region": "eastus2"}`. Assert 200 (not 400).
   - `test_list_services_includes_realtime_after_put`: After PUT azure_openai_realtime, GET /services. Assert result contains service_name="azure_openai_realtime".
  </action>
  <verify>
    <automated>cd /Users/huqianghui/Downloads/1.github/AI-Coach-vibe-coding/backend && python3 -m pytest tests/test_connection_tester.py tests/test_azure_config_api_extended.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/test_connection_tester.py contains `test_dispatch_azure_openai_realtime`
    - backend/tests/test_connection_tester.py contains `test_dispatch_azure_content`
    - backend/tests/test_connection_tester.py contains `test_dispatch_unknown_service`
    - backend/tests/test_connection_tester.py contains `test_azure_avatar_real_success`
    - backend/tests/test_connection_tester.py contains `test_azure_content_understanding_success`
    - backend/tests/test_connection_tester.py contains `test_azure_realtime_success`
    - backend/tests/test_azure_config_api_extended.py contains `test_region_capabilities_eastus2`
    - backend/tests/test_azure_config_api_extended.py contains `test_region_capabilities_requires_admin`
    - backend/tests/test_azure_config_api_extended.py contains `test_put_azure_openai_realtime`
    - `pytest tests/test_connection_tester.py tests/test_azure_config_api_extended.py -x -v` exits 0
  </acceptance_criteria>
  <done>Connection tester fully tested with mocked HTTP for all 7 services, API tests verify region-capabilities endpoint and azure_openai_realtime PUT acceptance</done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/test_adapters_new.py tests/test_region_capabilities.py tests/test_connection_tester.py tests/test_azure_config_api_extended.py -x -v` all pass
- `ruff check tests/test_adapters_new.py tests/test_region_capabilities.py tests/test_connection_tester.py tests/test_azure_config_api_extended.py` exits 0
- No real Azure API calls made during tests (all mocked)
</verification>

<success_criteria>
- All 4 test files pass with 0 failures
- Adapter tests cover is_available, execute, and parse_voice_live_mode
- Region capabilities tests verify correct output for eastus2, westus, unknown, case-insensitive
- Connection tester tests verify dispatch for all 7 services plus individual test functions
- API tests verify region-capabilities endpoint returns correct data and requires admin
- API tests verify azure_openai_realtime can be saved via PUT
</success_criteria>

<output>
After completion, create `.planning/phases/07-azure-service-integration/07-06-SUMMARY.md`
</output>
