# Plan 17-03: Integration Wiring, Tests, Build Verification

## Goal
端到端集成验证：KB 配置 → Agent sync with MCPTool → 前后端联调 → 测试覆盖 → 构建通过。

## Tasks

### Task 1: Agent Sync Integration Test

Test full flow:
1. Create HCP profile (triggers agent create)
2. Add KB config to HCP
3. Verify agent re-sync includes MCPTool in tools
4. Remove KB config
5. Verify agent re-sync removes MCPTool

### Task 2: Frontend Component Tests

- KnowledgeTab: renders empty state, renders KB list, add/remove interactions
- ConnectKbDialog: connection select, KB select, submit flow
- HCP editor: Knowledge tab visible and navigable

### Task 3: HCP Table KB Column (Optional Enhancement)

Add Knowledge column to HCP profiles table showing:
- Badge with KB count (e.g., "2 KBs") or "No KB"
- Consistent with existing Voice & Avatar badge pattern

### Task 4: Seed Data

Add seed KB config for demo HCP profiles (if AI Search connection available).

### Task 5: Full Build Verification

```bash
# Backend
cd backend
ruff check . && ruff format --check .
pytest -v

# Frontend
cd frontend
npx tsc -b
npm run build
```

### Task 6: Visual Checkpoint

Verify Knowledge tab UI matches AI Foundry portal screenshots:
- "Add" dropdown with "Connect to Foundry IQ"
- Connection + KB selection dialog
- Connected KB list with remove button

## Verification
- All tests pass
- Both builds clean
- Knowledge tab functional end-to-end
